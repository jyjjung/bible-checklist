// next.config.js

module.exports = {
  reactStrictMode: true, // Enable React Strict Mode
  swcMinify: true, // Use SWC for minification (faster build times)
  images: {
    domains: ["your-image-domain.com"], // Specify allowed image domains, adjust if needed
  },
};
